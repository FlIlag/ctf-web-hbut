# 文件安全审计思路

## 文件删除
```php
unlink($path);
```
删除 install 目录下的 install.lock，触发重装修改管理员密码
寻找 del/delete/remove 关键词，检查是否限制路径

## 目录穿越
```
/../../../etc/passwd
..\..\..\..\windows\win.ini
```
过滤了 `../` 但没过滤编码：`%2e%2e%2f`
过滤了 `../` 但能写绝对路径：`/etc/passwd`

## 文件写入
```
file_put_contents($path, $data);
```
可控 path 或 content → 写 webshell

## 任意文件下载
```
download.php?file=xxx
```
测试 file 参数是否可改为 `../../../etc/passwd`

## 审计关注
```
搜索关键词：
  $_GET['file']  $_POST['file']  $_GET['path']
  unlink(        file_put_contents(   fopen(   fwrite(
检测参数是否可控、是否过滤了路径穿越
```
