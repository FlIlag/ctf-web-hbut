# 支付逻辑审计

## 敏感功能点
追踪变量传递：从价格参数输入到支付完成的全链路
定位价格计算关键环节

## 测试参数
```
price           — 商品/服务价格
coupon_price    — 优惠券抵扣
discount        — 折扣
points_discount — 积分抵扣
quantity        — 购买数量
deposit         — 定金
shipping_fee    — 运费
total_price     — 总价
```

## 攻击方法
### 数量为负
```http
POST /order/create
quantity=-1&price=100
# 总价为 -100，越买越"赚钱"
```

### 数量极大
```http
quantity=999999999
# 整数溢出，可能变为 0 或负数
```

### 修改价格参数
```http
POST /order/create
price=0.01
# 直接改商品单价
```

### 修改状态参数
```http
POST /pay/callback
order_id=123&status=success&amount=100
# 伪造支付回调，直接修改订单状态
```

## 审计关注
```php
// 关注支付金额是否由前端传入
$_POST['total_price']
$_POST['amount']

// 关注回调是否验证签名
if ($_POST['status'] == 'success') { updateOrder(); }
// 缺少签名验证 → 可伪造回调

// 关注库存扣减是否有锁
// 缺少事务锁 → 竞争条件超卖
```
