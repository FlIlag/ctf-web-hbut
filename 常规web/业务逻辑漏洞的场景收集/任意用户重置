# 任意用户密码重置

## 验证码爆破
手机号只有 4 位未知 + 验证码 4 位无图形验证码 → 可爆破
步骤：遍历手机号后 4 位 + 爆破验证码

## 未绑定手机号
遍历用户名，寻找未绑定手机号的账号
系统可能给默认密码：admin/123456

## Token 可预测
```http
GET /reset?token=md5(uid)  → 遍历 uid 重置任意用户
GET /reset?token=timestamp  → 时间戳可预测
```

## 验证码返回前端
验证码在响应包中，直接获取填入

## 修改请求参数
```http
POST /send_code
phone=13600000001  → 改为 13600000002
# 验证码发到自己手机上

POST /reset
code=1234&uid=1  → 改 uid=2 重置他人
```
