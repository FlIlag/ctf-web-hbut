# 垂直越权

## 修改角色
注册时拦截请求包，修改 role 为 super_admin/admin
登录返回包修改 role、type 等权限字段

## Token 替换
高权限操作接口换低权限 token 测试
低权限 token 能否访问管理员接口

## 路径拼接
登录前后尝试功能点路径拼接
```http
/admin/user/list
/admin/system
```

## Json 功能点替换
登录时拦截返回包，包含功能点菜单的 json
把普通用户的菜单 json 替换为管理员的菜单 json

## pagesize
信息页的 pagesize 参数赋大值（100000）获取更多数据
可能泄露越权信息

## 找回密码暴露信息
遍历用户名，返回包暴露手机号/部分手机号
手机号 4 位未知 + 验证码 4 位 → 可爆破
改包换手机号实现任意用户密码修改
