# 暴力破解绕过

## Cookie 绕过
后台通过 cookie 判断登录错误次数
修改 cookie 中的参数可绕过次数限制
清除 cookie 重新请求

## Session_ID 绕过
session_id 有生成算法
随机生成新的 session_id 重置错误计数

## 请求对比
不同账号请求对比请求包和返回包
猜解后台验证参数
无区别时说明通过 cookie 判断，清除 cookie 即可

## IP 伪造绕过
```http
X-Forwarded-For: 127.0.0.2  # 每次换 IP
```
