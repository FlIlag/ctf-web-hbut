# 水平越权

## 常见特征
路径中带数字：`/1234/detail` → 遍历数字
参数中带 id：`?id=1234` → 遍历 id
base64/可解密字符串 → 解码后修改再编码

## 操作接口改包
在添加/修改/删除接口改 id，操作他人数据
判断依据：返回 200 vs 无权限 vs 400

## 参数置空
请求包参数置空或删除参数
可能导致敏感信息泄露或报错返回全部数据

## 测试方法
```
A 账号创建一条数据，记录 ID
B 账号尝试访问/修改该 ID
遍历参数中的数字/字符串
```
