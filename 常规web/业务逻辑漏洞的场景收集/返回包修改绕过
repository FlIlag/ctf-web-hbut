# 返回包修改绕过

## 响应码修改
```http
HTTP/1.1 403 Forbidden → 改 200 OK
{"code":403} → {"code":200}
```

## 状态字段修改
```json
{"success":false} → {"success":true}
{"msg":"error"} → {"msg":"success"}
{"status":"fail"} → {"status":"success"}
```

## Json 功能点替换
登录时拦截返回包
将普通用户的功能点 json 替换为管理员的功能点 json

## 验证码绕过
输入错误验证码，修改返回包 msg 绕过
