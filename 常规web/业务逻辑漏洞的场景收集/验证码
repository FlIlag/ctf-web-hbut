# 验证码漏洞

## Session 中获取
验证码放入 session 中，可通过特定接口获取

## Cookie 绕过
第一次登录无需验证码
错误几次后才要求验证码 → 用第一次的 cookie 继续爆破

## 返回包泄露
验证码存在于返回包中（可能加密）
重点看 json 中的 value、yzm、code 字段

## 群发同码
用数组接收手机号，两个手机号验证码相同
一个填自己的，一个填目标的，验证码共用

## 短信轰炸
纵向轰炸：同一手机号多次发送
横向轰炸：不同手机号发送
接口标记绕过：type=1 注册、type=2 找回、type=3 修改，轮换 type
参数传错时查看报错信息获取正确 type 列表

## 返回包校验
输入错误验证码，修改返回包 msg/mes 绕过

## 首次验证
验证码只在第一次输入时验证，后续未验证

## 置空绕过
验证码请求包置空或删掉验证码参数
