验证码放入seesion中，导致验证码可获得

对于第一次登录不需要图片验证码，错误了几次后要求图片验证码的
网站，很有可能通过更改cookie进行判断你是否错误，往往错误几次后
cookie会发生改变，所以可以尝试用第一次的cookie进行图片验证码爆破

验证码存在于返回包中，但可能加密，或者放在了数据头部分,重点看返回包json中的value部分或者yzm

有的开发人员会使用数组接收手机号，然后一起对手机号进行发送验证码，这个
时候两个手机号对应的验证码都是一样的，所以我们可以输入两个手机号，其中
有一个自己的，一个是别人的，自己收到的验证码和别人的验证码是一样的。

短信轰炸：纵向轰炸，同一个手机号轰炸；横向轰炸：不同手机号轰炸
利用空格绕过短信条数限制，修改cookie值绕过短信次数
利用接口标记绕过短信限制：发送短信验证，可能会设置参数值的不同，来判断是执行api什么样的功能。比如type=1是注册，type=2是忘记密
码，type=3是修改密码等。我们可以通过修收参数值，来绕过一分钟内只发送一次限制，达到短信轰炸的目的

短信轰炸：每个type或模式可以发1次，但不知道模式总共有多少或者模式的字符串为什么，可以尝试输入一个错误模式比如XXX，查看
报错信息，其中可能会有正确的模式的列表，获取列表中的数据在进行测试。

验证码部分也可能是返回包效验，尝试输入一个错误验证码，修改返回包msg或mes，从而绕过验证码

图片验证码只有第一次输入时进行验证，后续发包未验证验证码