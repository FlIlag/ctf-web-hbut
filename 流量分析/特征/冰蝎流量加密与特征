# 冰蝎流量特征

## 加密机制
AES + Base64，密钥动态生成（先 RSA 加密传输密钥）

## 请求特征
```
Accept: application/json, text/javascript, */*; q=0.01
User-Agent(默认): Java/1.8.0_201
Content-Type: application/x-www-form-urlencoded
```

## 连接三阶段
1. 密钥建立（GET，响应含 k=）
2. 测试连接（POST，固定返回格式）
3. 执行命令（POST，AES 加密）

## 请求/响应体
- 键值对，值为一长串 Base64
- 解密后为序列化对象（Java/PHP）
- Content-Length 固定或规律变化
- 响应为 Base64 密文

## 冰蝎4.x
支持 WebSocket、可选 AES/XOR/ROT

## 检测
- 密钥协商阶段必须存在（除非固定密钥）
- 请求长度固定，信息熵 > 6.0
- 固定时序：密钥协商→测试连接→执行命令
- 所有特征均可魔改，不能单点判断

## 参考
https://cn-sec.com/archives/3442690.html
https://cn-sec.com/archives/4283585.html
