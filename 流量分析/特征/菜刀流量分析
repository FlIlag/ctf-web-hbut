# 菜刀（Cknife / 中国菜刀）流量分析

## 概述

中国菜刀（Cknife）是最早的 Webshell 管理工具之一，现已被冰蝎、哥斯拉、蚁剑等替代。菜刀流量特征最为明显，容易被检测。

## 流量特征

### 1. 请求体特征

菜刀使用 **Base64** 对命令进行编码，编码方式简单：

```
POST 请求参数示例:
z1=base64编码的命令&z2=base64编码的其他参数
```

### 2. 核心特征——密码参数

菜刀默认的连接密码参数名：
```
c、pass、cmd、action、command、z0、z1、z2 等
```

### 3. User-Agent 特征

```
User-Agent: Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1)
```
极其陈旧的 User-Agent，非常可疑。

### 4. 请求体编码示例

菜刀发送的命令经过 Base64 编码：

原始的 POST body：
```
z1=Y2QgL2QgIkM6XG1hbHdhcmVcIgpkaXI=&z2=dGVzdF9jb25uZWN0aW9u
```

Base64 解码后对应命令：
```
cd /d "C:\malware"
dir
```

### 5. 固定请求模式

菜刀固定发送以下请求类型：
1. **文件列表** — 请求指定目录的文件列表
2. **文件读取** — 读取指定文件内容
3. **文件写入** — 写入小文件
4. **命令执行** — 执行系统命令
5. **数据库连接** — MySQL/MSSQL 连接
6. **虚拟终端** — 交互式终端

### 6. 返回包特征

菜刀的返回数据结构固定：
```
响应内容以一个固定字符串开头 + Base64编码内容 + 固定字符串结尾
如: ->|base64encoded|<-
```

## 菜刀的绕过方式

### 1. 参数名修改
```
将 z1, z2 修改为无特征参数名，如 file_id, page_no
```

### 2. 编码方式增强
```
在 Base64 基础上增加：
- 多重 Base64 编码
- 自定义异或加密
- URL 编码嵌套
```

### 3. 响应格式修改
```
修改返回格式，去掉 ->||<- 特征，改为 JSON 格式
或与其他正常业务请求格式一致
```

## 菜刀 vs 其他工具对比

| 特征 | 菜刀 | 冰蝎 | 哥斯拉 | 蚁剑 |
|------|------|------|--------|------|
| 加密 | Base64 | AES+RSA | XOR+AES | Base64+编码器 |
| 更新状态 | ❌ 已停更 | ✅ 持续更新 | ✅ 持续更新 | ✅ 持续更新 |
| 跨平台 | 部分 | ✅ Java | ✅ Java | ✅ 全平台 |
| 流量特征 | 极明显 | 较隐蔽 | 较隐蔽 | 取决于编码器 |
| 自定义能力 | 弱 | 强 | 强 | 极强 |

## 参考链接
- https://www.cnblogs.com/smileleooo/p/18177707
- https://cn-sec.com/archives/4283585.html
