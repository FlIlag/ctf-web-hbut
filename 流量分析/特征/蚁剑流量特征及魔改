# 蚁剑流量特征

## User-Agent（默认）
```
antsword_* / antSword/v2.1
```
特征极明显，修改即可绕过

## 参数
```
ant=base64编码内容  （默认参数名 ant）
```

## 编码器机制
```
default — 仅 Base64
base64  — base64_encode()
chr     — chr() 字符转换
rot13   — ROT13
cquote  — 字符拼接
```

## 魔改手段
- 修改 UA（core/module/request.js）
- 修改参数名 ant→ 无特征参数
- 自写编码器（多次Base64/随机填充/动态函数/XOR）
- 重命名模块文件

## 检测
- 静态：`eval($_POST)` / `base64_decode`+`eval` / Chr 混淆
- 动态：Base64 解码含 eval/system 关键字、参数名固定

## 参考
https://www.freebuf.com/articles/web/264896.html
https://cn-sec.com/archives/4283585.html
